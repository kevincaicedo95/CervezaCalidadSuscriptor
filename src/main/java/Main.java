import Servicio.Subscriptor;

public class Main {
    public static void main(String[] args) throws InterruptedException {
        Subscriptor subscriptor = new Subscriptor();
        subscriptor.leerMensaje();
    }
}
